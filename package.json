{
  "name": "first-assignment",
  "version": "1.0.0",
  "description": "TypeScript is a super version of JavaScript because it have extra features that JavaScript doesn't have. TypeScript has features like interfaces and classes, which make it easier to organize and structure code.",
  "main": "index.js",
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only problem_5.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  }
}
